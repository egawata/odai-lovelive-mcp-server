# MCP Server 動作モード
# stdio: Claude Desktop などのローカルクライアント用 (デフォルト)
# http: リモートサーバで公開する場合
MCP_MODE=stdio

# HTTP モード設定 (MCP_MODE=http の場合のみ有効)
# サーバが待ち受けるポート番号
PORT=3000

# サーバが待ち受けるホスト
# localhost: ローカルのみ (nginx などのリバースプロキシ経由を想定)
# 0.0.0.0: すべてのネットワークインターフェース (非推奨、nginx 経由が推奨)
HOST=localhost

# レート制限設定
# 制限をかける時間窓 (ミリ秒)
RATE_LIMIT_WINDOW_MS=60000

# 時間窓内の最大リクエスト数
RATE_LIMIT_MAX_REQUESTS=100
